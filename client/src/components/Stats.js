import React from 'react';
import StyledTopContainer from '../styles/StyledTopContainer'

const Stats = (props) => {
    return (
        <StyledTopContainer>
            Invoice Stats
        </StyledTopContainer>
    )
}

export default Stats
